import React from 'react';
import {
  ButtonCont,
  FinishedButton,
} from '../../styles/plantillaUnoStyle';

const WalkedButton = () => (
  <ButtonCont>
    <FinishedButton>
      End walk
    </FinishedButton>
  </ButtonCont>
);

export default WalkedButton;
