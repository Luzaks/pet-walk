import React from 'react';
import {
  ButtonCont,
  WalkButton,
} from '../../../styles/plantillaUnoStyle';

const StartButton = () => (
  <ButtonCont>
    <WalkButton>
      Start a walk
    </WalkButton>
  </ButtonCont>
);

export default StartButton;
